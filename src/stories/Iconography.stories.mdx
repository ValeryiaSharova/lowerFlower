import { IconGallery, IconItem, Meta } from '@storybook/addon-docs/';

import { Icon as IconExample } from 'src/components/atoms/Icon';
import * as icons from 'src/components/atoms/Icon/icons';

<Meta title="Global/Iconography" />

# Icon Gallery

<IconGallery>
  {Object.keys({
    ...Object.fromEntries(
      Object.entries(icons).filter((objKey) => objKey[0] !== 'iconsAnt')
    ),
    ...icons.iconsAnt,
  })
    .sort((a, b) => String(a).localeCompare(String(b)))
    .map((name) => (
      <IconItem name={name} key={name}>
        <IconExample icon={name} fill="#2e3240" size={30} />
      </IconItem>
    ))}
</IconGallery>
